import 'package:flutter/material.dart';

class RequestedUsers extends StatelessWidget {
  const RequestedUsers({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
