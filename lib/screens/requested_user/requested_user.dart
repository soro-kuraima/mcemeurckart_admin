import 'package:flutter/material.dart';

class RequestedUser extends StatelessWidget {
  const RequestedUser({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
